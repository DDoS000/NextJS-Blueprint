{
    "name": "@blueprint/db",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "db:generate": "bunx prisma generate",
        "db:migrate": "bunx prisma migrate dev",
        "db:push": "bunx prisma db push",
        "db:studio": "bunx prisma studio",
        "type-check": "tsc --noEmit"
    },
    "dependencies": {
        "@prisma/client": "^6.3.0"
    },
    "devDependencies": {
        "@blueprint/typescript-config": "workspace:*",
        "@types/node": "^20",
        "prisma": "^6.3.0",
        "typescript": "^5.7.0"
    }
}